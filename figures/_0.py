"""Zero module(base figure)."""
from figures.base import base

class zero(base):
    """Zero class."""

    def __init__(self, marks:dict):
        """Return base figure."""
        super().__init__(marks)

